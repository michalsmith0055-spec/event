# Compliance Notes
- Amazon scraping is intentionally excluded.
- Amazon source mode is centrally managed in `AppSetting.amazonSourceMode`.
- Affiliate disclosures are required by default (`requireDisclosure=true`).
- Creative generation uses user-uploaded assets, generated templates, or approved external URLs only.
- Pinterest OAuth is required before publishing.
- Duplicate cooldown and scheduling throttles are enforced in publish workflows.
